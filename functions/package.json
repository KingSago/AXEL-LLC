{
  "name": "axels-tracker-functions",
  "description": "Cloud Functions for Axel's lawn tracker (Stripe billing + webhooks)",
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions,firestore,auth,hosting",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^7.2.5",
    "stripe": "^16.12.0"
  },
  "private": true
}
